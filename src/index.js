import joeMap from './scripts/joeMap';
import tempData from './scripts/joeData'


document.addEventListener('DOMContentLoaded', ()=>{
    // const bob = bob();
    const mapDiv = document.getElementById('chart-container');
    mapDiv.appendChild(joeMap());
    // let node = data;
    // // console.log(data)
    // let myChart = chart(node)
    // let chartDiv = document.getElementById("chart-container")
    // chartDiv.appendChild(myChart)

});